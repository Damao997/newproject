/** 筛选控件宽度语义常量：主体选择 / 期间选择（各页统一引用，替代硬编码宽度） */
export const FILTER_WIDTH = {
  /** 主体/公司多选 */
  subject: 'w-[180px]',
  /** 期间单选 */
  period: 'w-[140px]',
  /** 中宽（搜索框等） */
  medium: 'w-[200px]',
} as const
