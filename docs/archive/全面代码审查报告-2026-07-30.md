# 浙江壹品慧财年经营数据分析平台 — 全面代码审查报告

> ⚠️ **本报告部分结论已被复核更正，正文不再修订。**
> 需注意的更正项：①§1.4/§5.3「数据模型规范未覆盖新字段」—— 该规范当日已升 v1.1 并在附录 D 记录，多数已同步；②§3.2/§4.1「64 个文件未提交」—— 已提交解决；③§1.5 magic number 校验位置实为 `services/ImportService.ts:assertExcelMagic()`；④§4.2 第 8 项「trend-card.tsx L145 硬编码金额」—— 该处为 **CSV 导出**数值字段，加千分位会破坏逗号分隔，**不应修改**；⑤§4.2 第 8 项 import-dialog 的正确修法是保留 `/10000` 元→万换算（往来数据单位为元，全局 `formatMoney` 不做换算）。
> **完整更正与后续处置见** `docs/archive/文档与代码差异对齐报告-2026-07-30.md` **§〇 与 §二**。

> **审查日期**：2026-07-30
> **审查范围**：全栈代码库（server/ + web/）+ Prisma Schema + 配置文��� + 文档对齐性
> **审查方法**：代码级静态审查 + 工具辅助扫描 + 文档对照分析
> **审查依据**：CLAUDE.md / 整体方案v3.md / 前端设计方案v3.md / 数据模型规范.md / 安全与权限规范.md / AI模块规范.md / 部署运维规范.md / 前端全面审查报告-2026-07-27.md / 项目交接文档-2026-07-26.md

---

## 总体评分

| 维度 | 评分 | 说明 |
|------|:----:|------|
| **文档-代码一致性** | ⚠️ 7/10 | 核心架构一致，但存在若干关键偏差 |
| **代码质量** | ✅ 8/10 | 整体质量良好，少量残留代码 |
| **安全规范遵循** | ✅ 9/10 | 中间件链完整，但有一处路由缺少权限声明 |
| **数据模型一致性** | ⚠️ 7/10 | 基础表结构匹配，部分字段类型与规范有偏差 |
| **部署就绪度** | ❌ 2/10 | 部署物料全部缺失 |
| **无用代码** | ⚠️ 少量 | Vite 模板残留 + mock 数据可清理 |

---

## 一、文档与代码一致性检查

### 1.1 CLAUDE.md 一致性

| 检查项 | 规范要求 | 实际状态 | 结论 |
|--------|----------|----------|:----:|
| 命名规范：前端变量camelCase | camelCase | `formatMoney`、`userList` 等全部符合 | ✅ |
| 命名规范：数据库列snake_case | `@map` 映射 | schema.prisma 全部使用 `@map("snake_case")` | ✅ |
| 命名规范：文件名kebab-case | `import-service.ts` | 全部符合（如 `kpi-card.tsx`） | ✅ |
| 命名规范：React组件PascalCase | `DataTable` | 全部符合 | ✅ |
| 命名规范：API路径kebab-case | `/api/v1/data/imports` | 全部符合 | ✅ |
| 金额展示：formatMoney带"万"后缀 | `formatMoney(value)` | 已实现（L8） | ✅ |
| 金额展示：formatMoneyWan纯数值 | `formatMoneyWan(value)` | 已实现（L16） | ✅ |
| 涨跌色标：红涨(#FF3B30)/绿跌(#34C759) | 正红负绿 | `getChangeColor` 符合 | ✅ |
| 精度：金额2位/百分比2位（已改1位） | 2位→1位小数 | `formatPercent` 已改为 `toFixed(1)` | ✅ |
| AI交互铁律：禁止直接覆盖编辑器 | SSE预览→确认→替换 | 代码逻辑符合 | ✅ |
| AI双管道：polish/analyze | 双管道架构 | `AIProxyService` 实现两个方法 | ✅ |
| 公式计算：禁止eval | 白名单算子解析 | AggregationService 白名单校验 | ✅ |
| 资源三段式编码：`模块:子页面:操作` | 三段式 | 路由全部使用三段式 | ✅ |
| 中间件执行链顺序 | 8步链 | 实现顺序一致 | ✅ |

**发现的不一致**：

| 严重度 | 文件 | 问题 | 建议 |
|:------:|------|------|------|
| P1 | `web/src/App.tsx` L42 | `tools` 路由使用 `resource="tools"`，但 CLAUDE.md 和 安全与权限规范.md 均未定义 `tools` 模块的 resource 编码 | 补充 `tools:view` resource 编码到文档中 |
| P1 | `docs/plans/AI模块规范.md` §附录A | 写模型名为 `deepseek-chat`，但实际配置（`env.ts` L67）默认为 `deepseek-v4-flash` | 更新文档与代码对齐（deepseek-v4-flash 为正确模型名） |
| P2 | `docs/plans/整体方案v3.md` §3.5 | 前端 UI 决策称"图标库 Lucide React"已实现，但 `docs/plans/部署运维规范.md` 附录A 仍列出 `@ant-design/icons` | 删除 `@ant-design/icons` 引用，与代码（零引用）一致 |

### 1.2 整体方案v3.md 一致性

| 检查项 | 规范要求 | 实际状态 | 结论 |
|--------|----------|----------|:----:|
| 8大模块定义 | 登录/首页/财务/往来/存货/报告/数据/权限 | 代码中全部存在 | ✅ |
| P0范围：往来/存货/报告仅占位tab | 仅占位展示设计概要 | 往来/存货/报告已实现完整功能，**超出P0范围** | ⚠️ |
| 中间件链：helmet→cors→express.json→auth... | 8步链 | app.ts 装配顺序一致 | ✅ |
| 统一响应 `{code, data, message}` | 标准格式 | 含额外 `traceId` 字段（正向增强） | ✅ |
| 13条用户故事覆盖 | US-01至US-13 | 核心功能覆盖，已超出P0 | ✅ |
| 多出的 `tools` 模块 | 文档未定义 | 代码中有 `pages/tools/` 企业工商查询页面 | ⚠️ |

### 1.3 前端设计方案v3.md 一致性

| 检查项 | 规范要求 | 实际值 | 结论 |
|--------|----------|--------|:----:|
| `--primary` | `221 83% 53%`（品牌蓝 #2563EB） | `25 95% 53%`（橙色） | **❌ 不一致** |
| `--ring` | `221 83% 53%`（蓝） | `25 95% 53%`（橙） | **❌ 不一致** |
| `--success` | `142 76% 36%`（#16A34A） | `160 84% 39%` | **⚠️ 有差异** |
| `--background` | `0 0% 100%` | `0 0% 100%` | ✅ |
| `--foreground` | `222 84% 5%` | `222 84% 5%` | ✅ |
| `--border` | `214 32% 91%` | `214 32% 91%` | ✅ |
| `--muted` | `210 40% 96%` | `210 40% 96%` | ✅ |
| `--muted-foreground` | `215 16% 47%` | `215 16% 47%` | ✅ |
| `--radius` | `0.75rem` | `0.75rem` | ✅ |
| CSS动画：纯CSS @keyframes | 无第三方动画库 | globals.css 仅含 `fadeIn`/`shimmer` 等 CSS 动画 | ✅ |
| 图标库：Lucide React全站统一 | 无 @ant-design/icons | 代码中 `@ant-design/icons` 零引用 | ✅ |
| 代码分割：React.lazy() + Suspense | 路由级别懒加载 | App.tsx 全部用 `lazy()` + `Suspense` | ✅ |
| React Query staleTime: 5分钟 | `5 * 60 * 1000` | App.tsx L21 配置完全一致 | ✅ |

**关键发现**：`--primary` 和 `--ring` 使用橙色系而非规范定义的品牌蓝，这是一个有意的设计变更（前台审查报告中也未提及此变更），但文档 `frontend-design-proposal.md` 未同步更新。需确认此变更是故意为之还是配置错误。

### 1.4 数据模型规范.md 一致性

| 检查项 | 规范要求 | 实际状态 | 结论 |
|--------|----------|----------|:----:|
| **company** 表 | 14字段 | 额外含 `shortName`（varchar 100） | ⚠️ 规范未覆盖 |
| **account_subject** 表 | 12字段 | 额外含 `valueType` 枚举（amount/quantity/ratio） | ✅ 已同步（valueType 改造） |
| **period_dimension** 表 | 7字段 | 完全匹配 | ✅ |
| **counterparty** 表 | 8字段 | 额外含 `partyType`（default "external"） | ⚠️ 规范未覆盖 |
| **user** 表 | 13字段 | 含额外字段（`dataScopeCodes` JSONB） | ⚠️ 规范未覆盖 |
| **role** 表 | 9字段 | 含额外字段 `orgScopeBu` | ⚠️ 规范未覆盖 |
| **permission** 表 | 7字段 | 一致 | ✅ |
| **permission.action** | ENUM（view/create/update/delete/export/import） | 实际为 `String` 类型 | ⚠️ 类型偏差 |
| **import_batch** 表 | 17字段 | 含额外 `detailCount`/`coverageJson` 等 | ⚠️ 规范未覆盖 |
| **metric** 表 | 18字段 | 含额外 `aiSuggested`/`sourceAccountCodes` 等 | ✅ |
| **transaction_detail** | 11字段 | 含额外 age 字段（aging_1m~aging_3y_plus）+ `rawJson` | ⚠️ 规范未覆盖 |
| **audit_log** | 按规范实现了分区兼容 | id 为 UUID，含 module/action/detail/ip | ✅ |
| **AgingRecord** bucket_code | ENUM规范有6个区间（0_1M~2Y_PLUS） | 代码实现有6个 bucket | ✅ |
| **AiDesensitizeConfig** 表 | 存在 | schema 中已定义 | ✅ |
| **Alert** 表（预留） | 存在 | schema 中已定义 | ✅ |
| **Dimension** + **DimensionMember** | 存在 | schema 中已定义 | ✅ |
| **EnterpriseInfo** + **EnterpriseQueryLog** | 存在 | schema 中已定义 | ✅ |

**注意**：`formula_rule` 表曾在 migration 2 创建，但已在 migration `20260729100000_drop_formula_rule` 中删除。schema.prisma 文件从未包含 `FormulaRule` 模型，当前状态一致。

### 1.5 安全与权限规范.md 一致性

| 检查项 | 规范要求 | 实际状态 | 结论 |
|--------|----------|----------|:----:|
| 中间件执行链顺序 | 8步链 | app.ts 装配顺序一致 | ✅ |
| requirePermission 用于每个路由 | 全部非公开路由 | 9个路由模块中8个完全覆盖 | ✅ |
| `/indicators/periods` 权限声明 | 应有 `requirePermission` | **缺少**（仅返回固定期间列表，风险较低） | ⚠️ P2 |
| 默认拒绝中间件 | 未声明权限路由返回403 | 已实现（error-handler 中处理） | ✅ |
| CORS配置 | 仅允许前端 origin | `middleware/cors.ts` 已配置 | ✅ |
| Helmet HTTP安全头 | CSP/HSTS/X-Frame | `middleware/helmet.ts` 已配置 | ✅ |
| JWT双Token+轮转+黑名单 | access 15min + refresh 7day | AuthService 已实现 | ✅ |
| Scope注入（Prisma $extends） | 自动拦截 companyCode 查询 | `middleware/scope.ts` 已实现 | ✅ |
| 审计日志（audit.ts） | 异步后置写入 | 中间件中实现 | ✅ |
| Prompt注入防护（四层） | promptGuard.ts | 代码中存在完整实现 | ✅ |
| 文件上传MIME magic number校验 | multer + 文件头校验 | `lib/excel-import.ts` 中校验 | ✅ |
| 富文本净化sanitize-html | 存储前净化 | `lib/sanitize.ts` 已实现 | ✅ |

### 1.6 AI模块规范.md 一致性

| 检查项 | 规范要求 | 实际状态 | 结论 |
|--------|----------|----------|:----:|
| 唯一出口 AIProxyService | 禁止直接调用 DeepSeek | `deepseek.ts` 注释明确警告 | ✅ |
| 双管道（polish/analyze） | 分离实现 | AIProxyService 中实现 | ✅ |
| 脱敏：金额→区间 | 分9档 | desensitize.ts 已实现 | ✅ |
| 脱敏：公司名→代号 | 动态映射 | desensitize.ts 中 `buildCompanyMap()` | ✅ |
| 模型名 | `deepseek-chat`（规范）vs `deepseek-v4-flash`（代码） | **文档与代码冲突** | **❌** |
| 频率限制 | 单用户10次/分 + 全局100次/分 | `middleware/ai-rate-limit.ts` 已实现 | ✅ |
| AI文本 `<div data-ai-suggested="true">` | 携带标识 | 需确认前端渲染组件是否适配 | ⚠️ |
| 公式生成：仅传科目结构不传数值 | 禁止传递 value | AIProxyService 实现符合 | ✅ |

### 1.7 部署运维规范.md 一致性

| 检查项 | 规范要求 | 实际状态 | 结论 |
|--------|----------|----------|:----:|
| docker-compose.yml | 应存在于根目录 | **不存在** | ❌ P0 |
| server/Dockerfile | 应存在于 server/ | **不存在** | ❌ P0 |
| web/Dockerfile | 应存在于 web/ | **不存在** | ❌ P0 |
| nginx.conf | 应存在 | **不存在** | ❌ P0 |
| init-roles.sql | 应存在于 server/prisma/ | **不存在** | ❌ P0 |
| backup.sh | 应存在 | **不存在** | ❌ P1 |
| 环境变量清单对齐 | .env 与规范一致 | `FRONTEND_ORIGIN` 支持多值（已增强） | ✅ |

---

## 二、无用代码/文件识别

### 2.1 已确认删除（审查报告验证通过）

| 文件 | 状态 | 验证方式 |
|------|:----:|----------|
| `web/src/index.css` | ✅ 已删除 | 目录扫描确认不存在 |
| `web/src/App.css` | ✅ 已删除 | 目录扫描确认不存在 |

### 2.2 建议删除/清理的文件

| # | 文件路径 | 大小 | 建议理由 |
|---|----------|:----:|----------|
| 1 | `web/assets/react.svg` | 4KB | Vite 模板残留，代码中零引用 |
| 2 | `web/assets/vite.svg` | 9KB | Vite 模板残留，代码中零引用 |
| 3 | `web/assets/hero.png` | 13KB | Vite 模板残留/设计历史，代码中零引用 |
| 4 | **`web/logo.png`** | 65KB | 根目录下的图片文件，未在代码中引用（public/ 下有完整图标集） |
| 5 | **`web/demo.html`** | 23KB | 根目录下的演示页面，与项目无关 |
| 6 | `web/src/mock/data.ts` | 10KB | Mock 数据，生产构建不包含但可能造成混淆，建议归档到 `docs/data-templates/samples/` |
| 7 | `web/src/mock/operating-analysis.ts` | 7KB | Mock 数据，同上 |
| 8 | `web/src/mock/static-analysis.ts` | 2KB | Mock 数据，同上 |
| 9 | `server/.pgdata/` | — | 嵌入式 PostgreSQL 数据目录，git 应忽略（已在 `.gitignore` 中） |

### 2.3 硬编码金额格式（未收敛到统一函数）

| # | 文件 | 行号 | 代码 | 建议 |
|---|------|:----:|------|------|
| 1 | `web/src/pages/transactions/import-dialog.tsx` | L17-18 | `if (Math.abs(v) >= 10000) return \`${(v / 10000).toFixed(2)}万\`` | 使用全局 `formatMoney` / `formatMoneyWan` |
| 2 | `web/src/pages/transactions/trend-card.tsx` | L145 | `v.toFixed(2)` | 使用全局格式化函数 |

---

## 三、知识问题验证（基于两份审查报告）

### 3.1 前端全面审查报告-2026-07-27.md 验证

| # | 报告问题 | 状态 | 验证结果 |
|---|----------|:----:|----------|
| 1 | P0 `subject-tree-panel.tsx` FilterParams 缺少 `includeInactive` | ✅ **已修复** | 类型已补充 |
| 2 | P2 `index.css` 和 `App.css` 死文件 | ✅ **已删除** | 确认不存在 |
| 3 | P2 `transactions/index.tsx` 硬编码 `(v/10000).toFixed(2)万` | ⚠️ **部分修复** | 原文件 `transactions/index.tsx` 中未找到该用法，但 `import-dialog.tsx` L17-18 和 `trend-card.tsx` L145 存在类似问题 |
| 4 | P2 KPI卡片 `data.unit === '%'` 分支隐式约定 | ⚠️ **未修复** | 代码仍依赖隐式约定 |
| 5 | P2 `vite.config.ts` vendor-charts(1.1MB)/vendor-excel(938KB)超500KB | ⚠️ **未修复** | 配置未变更 |
| 6 | P3 `metric-values.ts` 无单元测试覆盖 | ⚠️ **未修复** | 仍无测试文件 |
| 7 | formatPercent 改为 toFixed(1) | ✅ **已修复** | 确认 `formatPercent` 为 `toFixed(1)` |
| 8 | font-num 字体族定义 | ✅ **已修复** | `tailwind.config.js` 中已定义 |
| 9 | 首页与数据预览去"万"字 | ✅ **已修复** | 已使用 `formatMoneyWan` |
| 10 | 科目 valueType 分型展示 | ✅ **已修复** | schema 含 `ValueType` 枚举 + 种子打标 |

### 3.2 项目交接文档-2026-07-26.md 验证

| # | 交接文档问题 | 状态 | 验证结果 |
|---|------------|:----:|----------|
| 1 | **64个文件未提交** | ⚠️ **仍存在** | 工作目录仍有大量未提交变更 |
| 2 | **部署物料缺失** | ❌ **仍缺失** | docker-compose.yml/Dockerfile/nginx.conf/backup.sh/init-roles.sql 全部不存在 |
| 3 | **前端生产构建未验证** | ⚠️ **未知** | 未执行构建验证 |
| 4 | 文档对齐：README称财年4月vs代码可配置 | ✅ **已确认** | FISCAL_START_MONTH 变量已实现 |
| 5 | T26主题：毛玻璃→纸质感 | ✅ **已切换** | globals.css 为纸质感风格 |

---

## 四、潜在技术债务与重构建议

### 4.1 高优先级（P0/P1）

| # | 领域 | 问题 | 建议 | 预计工作量 |
|---|------|------|------|:----------:|
| 1 | **部署** | 全部部署物料缺失（docker-compose/Dockerfile/nginx/backup/init-roles） | 按 `docs/plans/部署运维规范.md` 创建全部部署文件 | 1-2人日 |
| 2 | **提交管理** | 64个文件(+5024/-544行)未提交 | 按功能模块拆分提交 | 0.5人日 |
| 3 | **前端构建验证** | 生产构建未验证 | 运行 `cd web && npm run build` | 0.1人日 |
| 4 | **测试运行** | 后端测试状态未知 | 运行 `cd server && npm test` | 0.1人日 |

### 4.2 中优先级（P1/P2）

| # | 领域 | 问题 | 建议 |
|---|------|------|------|
| 5 | **Design Token** | `--primary` 和 `--ring` 使用橙色而非规范蓝色 | 对齐规范或更新文档，二选一 |
| 6 | **文档同步** | AI模块规范模型名写 `deepseek-chat` 但代码用 `deepseek-v4-flash` | 更新规范文档对齐代码 |
| 7 | **路由安全** | `/indicators/periods` 缺少 `requirePermission` | 添加 `requirePermission('indicators:view', 'view')` |
| 8 | **格式化统一** | `transactions/import-dialog.tsx` 和 `trend-card.tsx` 硬编码金额格式 | 收敛到 `lib/utils.ts` 的统一格式化函数 |
| 9 | **文档体系** | `tools` 模块未在资源编码清单中定义 | 补充 `tools:*` resource 编码 |
| 10 | **Chunk瘦身** | vendor-charts(1.1MB)和vendor-excel(938KB)超500KB | ECharts 按需引入，减少导出库体积 |
| 11 | **测试覆盖** | `metric-values.ts` 中 `calcYoy/calcAchievement/calcYtdYoy` 无单元测试 | 补充边界用例（分母为0、负值） |

### 4.3 低优先级（P3）

| # | 领域 | 问题 | 建议 |
|---|------|------|------|
| 12 | **清理** | `web/src/assets/` 下 react.svg/vite.svg/hero.png 为模板残留 | 删除 |
| 13 | **清理** | `web/logo.png` 和 `web/demo.html` 为模板残留 | 确认无用后删除 |
| 14 | **清理** | `web/src/mock/` 下 mock 数据文件 | 归档到 `docs/data-templates/samples/` |
| 15 | **代码约定** | KPI卡片 `data.unit === '%'` 的隐式约定 | 在 `KpiData` 类型注释中固化约定，或后端统一传0-1小数 |
| 16 | **文档一致性** | deployment 规范附录A仍列出 @ant-design/icons（代码已零引用） | 删除该条目 |

---

## 五、与设计规范偏离的实例汇总

### 5.1 架构层面偏离

| # | 规范描述 | 实际实现 | 影响 |
|---|----------|----------|------|
| 1 | 8模块：往来/存货/分析报告为P1/P2暂缓，前端仅占位tab | 全部已实现完整功能（超出P0范围） | 正面——功能更完整；但计划管理需更新 |
| 2 | 文档定义8模块 + 9路由模块 | 实际有 `tools`（企业工商查询）额外模块 | 需补充文档 |

### 5.2 前端层面偏离

| # | 规范要求 | 实际值 | 影响 |
|---|----------|--------|------|
| 1 | `--primary: 221 83% 53%`（品牌蓝） | `--primary: 25 95% 53%`（橙色） | 全站主色变更，影响品牌一致性 |
| 2 | `@ant-design/icons` 已移除 | 部署运维规范附录A仍列该依赖 | 文档过时 |
| 3 | 金额格式化统一函数 | transactions 页2处硬编码 | 后续维护需同步修改 |

### 5.3 数据模型层面偏离

| # | 规范描述 | 实际实现 | 影响 |
|---|----------|----------|------|
| 1 | company 表规范14字段 | 含额外 `shortName` | 规范未及时更新 |
| 2 | counterparty 表规范8字段 | 含额外 `partyType` | 规范未及时更新 |
| 3 | `permission.action` 应为枚举 | 实际为 String | 类型安全性略低 |
| 4 | 规范无 `transaction_account` 模型 | schema 中存在（migration 新增） | 规范需更新 |

### 5.4 部署层面偏离

| # | 规范要求 | 实际实现 | 影响 |
|---|----------|----------|------|
| 1 | docker-compose.yml | 不存在 | **阻断交付** |
| 2 | server/Dockerfile | 不存在 | **阻断交付** |
| 3 | web/Dockerfile | 不存在 | **阻断交付** |
| 4 | nginx.conf | 不存在 | **阻断交付** |
| 5 | init-roles.sql | 不存在 | **阻断交付** |
| 6 | backup.sh | 不存在 | 备份功能缺失 |

---

## 六、关键度量指标

| 指标 | 数值 | 说明 |
|------|:----:|------|
| 后端TypeScript文件数 | ~90+ | 含 config/lib/middleware/routes/services/test/types |
| 前端TypeScript/TSX文件数 | ~60+ | 含 components/hooks/lib/pages/stores/types/test |
| Prisma模型数 | ~32个 | schema.prisma 中定义的模型 |
| Prisma迁移数 | 20个 | 从 init 到最新 migration |
| 后端测试状态 | `tsc --noEmit` 零错误 | 实际测试覆盖率待运行验证 |
| 前端测试 | 57个用例全部通过 | 来自前端审查报告 |
| 设计Token一致性 | 7/10个一致 | --primary/--ring/--success 存在偏差 |

---

## 七、操作建议（优先级排序）

### 立即执行（P0）

```bash
# 1. 提交未保存工作（按功能拆分）
git add -p  # 逐模块提交

# 2. 验证前端生产构建
cd web && npm run build

# 3. 运行后端测试
cd server && npm test
```

### 短期修复（P0/P1）

- 创建缺失的部署物料（docker-compose.yml + Dockerfile × 2 + nginx.conf + init-roles.sql + backup.sh）
- 确认 `--primary` 颜色选择的意图（橙色 vs 蓝色），文档同步更新
- 修复 `/indicators/periods` 缺少 requirePermission 的安全问题

### 中期（P1）

- 统一 transactions 页面的金额格式化
- 补充 model 规范文档（shortName/partyType/transaction_account 等新字段）
- 对齐AI模块规范中的模型名
- 清理 Vite 模板残留文件

---

*本审查报告基于 2026-07-30 代码仓库实测生成。核心发现已按 P0/P1/P2/P3 分级，方便决策修复顺序。*
