# 全站线条式（Line-style）Tab 改造实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将全站 7 文件 8 处基于 `components/ui/tabs.tsx` 的 Tab 统一改造为线条式（Line-style）：选中态主色短横线 + 未选中弱化 + 底部分割线。

**Architecture:** 共享组件层为 `TabsList`/`TabsTrigger` 新增 `variant='line'`（默认 `default` 零回归），`TabsList` 通过 React Context 将 variant 自动传播给内部 `TabsTrigger`；8 处调用点仅在 `TabsList` 传 `variant="line"` 并按需调整容器分割线。

**Tech Stack:** React 18 + Radix UI Tabs + Tailwind CSS（tailwind-merge via `cn()`）

**Spec:** `docs/superpowers/specs/2026-08-13-line-tabs-design.md`

---

### Task 1: 共享组件 tabs.tsx 增加 line 变体 + Context 传播

**Files:**
- Modify: `web/src/components/ui/tabs.tsx`

- [ ] **Step 1: 修改组件**

```tsx
import * as React from "react"
import * as TabsPrimitive from "@radix-ui/react-tabs"
import { cn } from "@/lib/utils"

const Tabs = TabsPrimitive.Root

type TabsVariant = 'default' | 'line'
/** TabsList 的 variant 自动传播给内部 TabsTrigger（TabsTrigger 可显式覆盖） */
const TabsVariantContext = React.createContext<TabsVariant>('default')

const TabsList = React.forwardRef<
  React.ElementRef<typeof TabsPrimitive.List>,
  React.ComponentPropsWithoutRef<typeof TabsPrimitive.List> & { variant?: TabsVariant }
>(({ className, variant = 'default', ...props }, ref) => (
  <TabsVariantContext.Provider value={variant}>
    <TabsPrimitive.List
      ref={ref}
      className={cn(
        "inline-flex h-10 items-center justify-center rounded-md bg-muted p-1 text-muted-foreground",
        variant === 'line' && "h-auto gap-x-5 rounded-none border-b border-border bg-transparent p-0",
        className
      )}
      {...props}
    />
  </TabsVariantContext.Provider>
))
TabsList.displayName = TabsPrimitive.List.displayName

const TabsTrigger = React.forwardRef<
  React.ElementRef<typeof TabsPrimitive.Trigger>,
  React.ComponentPropsWithoutRef<typeof TabsPrimitive.Trigger> & { variant?: TabsVariant }
>(({ className, variant: variantProp, ...props }, ref) => {
  const variant = variantProp ?? React.useContext(TabsVariantContext)
  return (
    <TabsPrimitive.Trigger
      ref={ref}
      className={cn(
        "inline-flex items-center justify-center whitespace-nowrap rounded-sm px-3 py-1.5 text-sm font-medium ring-offset-background transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 data-[state=active]:bg-background data-[state=active]:text-foreground data-[state=active]:shadow-sm",
        variant === 'line' &&
          "relative rounded-none bg-transparent px-1 py-2 text-muted-foreground shadow-none hover:bg-transparent hover:text-foreground data-[state=active]:bg-transparent data-[state=active]:text-primary data-[state=active]:shadow-none data-[state=active]:font-medium after:pointer-events-none after:absolute after:bottom-0 after:left-1/2 after:h-0.5 after:w-8 after:-translate-x-1/2 after:rounded-full after:bg-primary after:content-[''] after:opacity-0 after:transition-opacity data-[state=active]:after:opacity-100",
        className
      )}
      {...props}
    />
  )
})
TabsTrigger.displayName = TabsPrimitive.Trigger.displayName

// TabsContent 保持不变
```

- [ ] **Step 2: 验证类型检查**

Run: `cd web; npx tsc --noEmit`
Expected: 无错误

### Task 2: dashboard 综合看板 4 文件切换 line（贯穿式分割线）

**Files:**
- Modify: `web/src/pages/dashboard/analysis-tabs-card.tsx:41-43,68-74`
- Modify: `web/src/pages/dashboard/trend-section.tsx:28-60`
- Modify: `web/src/pages/dashboard/product-budget-card.tsx:67-77`
- Modify: `web/src/pages/dashboard/subject-budget-card.tsx:66-78`

- [ ] **Step 1: analysis-tabs-card.tsx** — 删除 `TAB_TRIGGER_CLS` 胶囊常量，TabsList 改 `variant="line" className="mb-4 w-full"`，TabsTrigger 删除 className

- [ ] **Step 2: trend-section.tsx** — 标题行容器追加 `border-b border-border`；两组 TabsList 改 `variant="line" className="border-b-0"`；TabsTrigger 删除胶囊 className

- [ ] **Step 3: product-budget-card.tsx / subject-budget-card.tsx** — 标题行容器追加 `border-b border-border`；TabsList 改 `variant="line" className="border-b-0"`；TabsTrigger 删除胶囊 className

- [ ] **Step 4: 验证** Run: `cd web; npx tsc --noEmit` Expected: 无错误

### Task 3: 其他页面 3 文件切换 line（组宽分割线）

**Files:**
- Modify: `web/src/pages/reports/index.tsx:130`
- Modify: `web/src/pages/admin/roles.tsx:414-423`
- Modify: `web/src/pages/data/import-compare-dialog.tsx:193`

- [ ] **Step 1: reports/index.tsx** — TabsList 改 `variant="line"`

- [ ] **Step 2: roles.tsx** — TabsList 改 `variant="line"` 并移除 `className="h-8"`；TabsTrigger 移除 `h-7`（保留 `gap-1 px-3 text-xs` 与图标）

- [ ] **Step 3: import-compare-dialog.tsx** — TabsList 改 `variant="line"`

- [ ] **Step 4: 验证** Run: `cd web; npx tsc --noEmit` Expected: 无错误

### Task 4: 回归验证

- [ ] **Step 1: Lint** Run: `cd web; npx oxlint .` Expected: 无新增错误

- [ ] **Step 2: 类型检查** Run: `cd web; npx tsc --noEmit` Expected: 通过

- [ ] **Step 3: 视觉走查**（启动 dev server）：
  - 首页综合分析卡：主 Tab 贯穿分割线 + 主色短横线 + 主题色联动
  - 趋势分析页/预算卡：子 Tab 短横线贴整行贯穿线
  - 报告/角色/导入比对：组宽分割线，布局不破
  - 角色页短横线无悬空
