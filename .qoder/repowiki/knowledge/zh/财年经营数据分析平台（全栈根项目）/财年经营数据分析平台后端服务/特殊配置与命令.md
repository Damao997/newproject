开发启动：`npm run dev`（tsx watch 热重载）；构建：`npm run build`（tsc）；数据库迁移：`npm run prisma:migrate`；种子数据：`npm run prisma:seed`；本地 Postgres：`npm run db:local`（embedded-postgres 17.10）；测试：`npm test` / `npm run test:coverage`；完整初始化：`npm run setup`。