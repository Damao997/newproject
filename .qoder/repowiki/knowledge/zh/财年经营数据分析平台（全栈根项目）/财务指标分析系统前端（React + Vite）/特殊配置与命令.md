开发启动 `npm run dev`（Vite 默认 5173 端口，自动代理 `/api/v1` 到 `http://localhost:3001`）；构建命令 `npm run build`（先 `tsc -b` 类型检查再 `vite build`）；测试 `npm run test` 或 `npm run test:coverage`；代码检查 `npm run lint`（Oxlint）。